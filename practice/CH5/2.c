#include <stdio.h>
#include <conio.h>
int main(void)
{
    int a = 0;
    if (a = 0)
        printf("a is zero\n");
    else
        printf("a is not zero\n");
    printf("Value of a is %d", a);
    return 0;
}
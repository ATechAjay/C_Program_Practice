#include <stdio.h>
#include <conio.h>
int main(void)
{
    int i = 10;
    i == 30;
    //i is not 30
    if (i == 30)
        printf("i is fifty\n");
    else
        printf("i is not fifty\n");
    printf("%d", i);
    return 0;
}